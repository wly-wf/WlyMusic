{
  "name": "wly-music",
  "version": "0.1.0",
  "description": "Win11 Local Music Player",
  "private": true,
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=src/frontend",
    "build": "npm run build --workspace=src/frontend",
    "electron": "npm run electron --workspace=src/frontend"
  }
}